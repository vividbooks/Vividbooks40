{
  "name": "vividbooks-figma-plugin",
  "version": "1.0.0",
  "description": "Figma plugin for syncing Vividbooks worksheets into Figma",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.101.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0"
  }
}
